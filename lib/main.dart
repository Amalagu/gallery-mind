import 'package:flutter/material.dart';

import 'app.dart';

void main() {
  // Every Flutter app starts here. runApp mounts the root widget into the
  // phone screen; GalleryMindApp then builds the MaterialApp and first page.
  runApp(const GalleryMindApp());
}
